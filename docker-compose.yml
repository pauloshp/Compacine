version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - mongo
    environment:
      - MONGO_URL=mongodb+srv://develop:2uxxsIBpBdaEr193@cluster0.iycazk9.mongodb.net/movies?retryWrites=true&w=majority&appName=Cluster0

  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
